{
  "buildCommand": "npm install --legacy-peer-deps && npm run vercel-build",
  "devCommand": "next dev",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "regions": ["sin1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
