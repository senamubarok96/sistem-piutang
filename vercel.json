{
  "buildCommand": "bash vercel-build.sh",
  "installCommand": "echo 'Skipping default install, handled in vercel-build.sh'",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./src ./package.json ./vercel.json ./vercel-build.sh || exit 1"
}
